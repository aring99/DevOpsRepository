---
- hosts: localhost
  connection: local
  become: true
  tasks:
    - name: 'Execute a date command'
      command: date     
    - name: 'Task to display nameservers'
      command: 'cat /etc/resolv.conf'  