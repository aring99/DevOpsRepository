- name: Remove Apache web server
  hosts: webservers
  connection: local
  become: true
  tasks:
    - name: Remove apache2
      ansible.builtin.apt:
        name: apache2
        state: absent
        purge: yes